package main;

public enum GameState {

     MENU_STATE,
     PLAY_STATE,
     PASSWORD_STATE,
     PAUSE_STATE,
     DIALOGUE_STATE,
     LEVELUP_STATE,
     LOSE_STATE,
     SETTING_STATE,
     QUIZ_STATE,
     WIN_STATE
}
