package org.kat.app.main;

public enum GameState {
     LOADING,
     MENU,
     PLAY,
     PASSWORD,
     PAUSE,
     UI_INGAME_OPENED,
     DIALOGUE,
     LEVELUP,
     LOSE,
     SETTING,
     QUIZ,
     WIN
}
